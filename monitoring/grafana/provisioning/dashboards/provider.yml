apiVersion: 1
providers:
  - name: EduCluster local
    orgId: 1
    folder: EduCluster
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

# services:

#   api:
#     build:
#       context: .
#       dockerfile: Dockerfile
#     container_name: educluster-api
#     environment:
#       CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5173,http://localhost:4173,http://localhost:8080}
#     ports:
#       - "8002:8000"
#     depends_on:
#       - mlflow
#     restart: unless-stopped

#   mlflow:
#     build:
#       context: .
#       dockerfile: Dockerfile.mlflow
#     container_name: educluster-mlflow
#     volumes:
#       - ./mlruns:/mlflow/mlruns
#     ports:
#       - "5001:5000"
#     restart: unless-stopped

#   dashboard:
#     build:
#       context: ./dashboard
#       dockerfile: Dockerfile
#       args:
#         VITE_API_URL: ${VITE_API_URL:-http://localhost:8002}
#         VITE_MLFLOW_URL: ${VITE_MLFLOW_URL:-http://localhost:5001}
#     ports:
#       - "${DASHBOARD_PORT:-8080}:80"
#     depends_on:
#       - api
#     restart: unless-stopped

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: educluster-api
    environment:
      CORS_ORIGINS: ${CORS_ORIGINS:-http://41.250.66.71:4203,http://localhost:4203}
    ports:
      - "4201:8000"
    depends_on:
      mlflow:
        condition: service_healthy
    restart: unless-stopped

  mlflow:
    build:
      context: .
      dockerfile: Dockerfile.mlflow
    container_name: educluster-mlflow
    volumes:
      - ./mlflow-data:/mlflow/data
    ports:
      - "4202:5000"
    restart: unless-stopped

  dashboard:
    build:
      context: ./dashboard
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ${VITE_API_URL:-http://41.250.66.71:4201}
        VITE_MLFLOW_URL: ${VITE_MLFLOW_URL:-http://41.250.66.71:4202}
    ports:
      - "${DASHBOARD_PORT:-4203}:80"
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped

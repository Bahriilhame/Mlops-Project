educluster:
  target: dev

  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('EDUCLUSTER_DUCKDB_PATH', 'oulad_pipeline.duckdb') }}"
      schema: main
      threads: 1
      settings:
        threads: 1
        memory_limit: '512MB'
